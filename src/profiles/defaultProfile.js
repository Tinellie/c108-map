export const defaultProfile = {
  name: "default",
  description: "Generic starter extraction for headings, links, and paragraphs",
  selectors: {
    headings: "h1, h2, h3",
    links: "a[href]",
    paragraphs: "p"
  }
};
